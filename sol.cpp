#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	int tt;
	cin >> tt;
	while (tt--) {
		int n;
		cin >> n;
		// calculate the midpoint from 1 to 'n'
		cout << (1 + n) / 2 << '\n';
	}
	return 0;
}
